spring:
  application:
    name: variation
  cloud:
    config:
      uri: http://localhost:8206/   #Config server的uri
      profile: auth                       #指定的环境
      label: master                        #指定分支

server:
  port: 8205

# 允许/actuator/refresh接口被外部调用
management:
  endpoints:
    web:
      exposure:
        include: "*"